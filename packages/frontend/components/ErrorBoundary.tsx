/**
 * Error boundary — re-exports Bloom's ErrorBoundary component.
 * Wraps children, catches render errors, and shows a fallback UI with retry.
 */

export { ErrorBoundary } from '@oxy.so/bloom/error-boundary';
